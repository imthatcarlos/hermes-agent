{
  "name": "hedge-fund-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Helper scripts for the hedge-fund Hermes skill — research (x402 calls to Coingecko + Checkr) + aggregator. Installed at container build time via the Dockerfile npm install step.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@x402/fetch": "^2.11.0",
    "@x402/evm": "^2.11.0",
    "viem": "^2.39.3"
  }
}
